numpy>=1.24
scipy>=1.10
matplotlib>=3.6
pytest>=7.0

# Interactive demo (app.py). streamlit<1.50 keeps the tornado-based server,
# which avoids a starlette version conflict on some setups.
streamlit>=1.40,<1.50
plotly>=5.18
