"""Interactive demo helpers for the food-chain chemostat model.

This subpackage wraps the analysis code in :mod:`foodchain` with

* cached, UI-friendly computations (:mod:`demo.compute`),
* Plotly figures and animations (:mod:`demo.viz`),
* "does it match the paper?" checks (:mod:`demo.insights`).

It is consumed by the Streamlit app in ``app.py``.
"""
